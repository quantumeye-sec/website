{
  "name": "quantumeye-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",            
    "predeploy": "rm -rf .next out && npm run build && node -e \"require('fs').writeFileSync('out/.nojekyll','')\"",
    "deploy": "gh-pages -d out --dotfiles --nojekyll"
  },
  "dependencies": {
    "next": "15.5.4",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.0.0",
    "autoprefixer": "^10.4.21",
    "gh-pages": "^6.3.0",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.13"
  }
}
